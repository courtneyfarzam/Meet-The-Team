const generateManager = () => {
    return
    `stuff`
}

const generateEngineer = () => {
    return
    `stuff`
}

const generateIntern = () => {
    return
    `stuff`
}

const generateTeam = (data) => {
    // You will need a for loop here, then if statement to get specific role
    // After looping, return array with populated cards.
}

const generateHTML = (teamRoster) => {
    let cardArray = generateTeam(teamRoster);
    return
    `stuff
    ${cardArray}`
    // This will actually create and compile the cards made in through the questions

}

module.exports = generateHTML;